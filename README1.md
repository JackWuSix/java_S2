yangtze@ERP-Yangtze-Test:~/yangtze-bench/apps/hrms$ git cherry-pick -m 1 36bcc1c1443610924f2d13145f2aaabd3ff1f173
On branch beumer-upgrade
Your branch and 'upstream/beumer-upgrade' have diverged,
and have 2 and 5 different commits each, respectively.

You are currently cherry-picking commit 36bcc1c.
  (all conflicts fixed: run "git cherry-pick --continue")
  (use "git cherry-pick --skip" to skip this patch)
  (use "git cherry-pick --abort" to cancel the cherry-pick operation)

nothing to commit, working tree clean
The previous cherry-pick is now empty, possibly due to conflict resolution.
If you wish to commit it anyway, use:

    git commit --allow-empty

Otherwise, please use 'git cherry-pick --skip'
yangtze@ERP-Yangtze-Test:~/yangtze-bench/apps/hrms$ 
